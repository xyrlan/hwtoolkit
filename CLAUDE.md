# CLAUDE.md — hwtoolkit project notes

Windows hardware-fingerprint spoofing toolkit: BOOT_START kernel filter driver (`driver/rstflt.c`, DiskDrive class UpperFilter) + PowerShell user-mode spoofers (`scripts/`) + sequential `NN-*.bat` pipeline in the repo root. Targets: Fase 5 anti-cheat detection surfaces (WMI, SMBIOS, CPU registry, MAC, EDID, disk serial, Windows machine identity). Primary threat model docs: `docs/emac-recon-v2.md`, `docs/fase2-kickoff.md`.

**As of Phase 1 (2026-08-31), Level A (EMAC-only userland spoof, no kernel driver) is the recommended default against EMAC-tier anti-cheats (RubinOT) per `docs/emac-recon-v3.md`.** Level C driver install (`03-instalar-driver.bat` + kernel replay) remains opt-in for future stronger anti-cheats that inspect SMBIOS/CPUID beyond what EMAC surfaces today.

**As of Track D (2026-09-01, driver v5.0.0), an ADD-ON to Level C exists: `CmRegisterCallbackEx`-based kernel rewrite of `Enum\SCSI\Disk&Ven_*` subkey names, targeting the three consecutive RubinOT/EMAC bans (baseline / Level A / fresh identity + PRs #12/#13/#14/#15) that confirmed H2 - EMAC reads NAMES via `RegEnumKeyEx`, not just values.** Opt-in via `.\scripts\track-d-arm.ps1 -Enable` after `03-instalar-driver.bat`. Design + safety contract: [`docs/track-d-kernel-registry-callback-kickoff.md`](docs/track-d-kernel-registry-callback-kickoff.md) + v5.0.0 changelog block at top of `driver/rstflt.c`.

**Language conventions in-repo**: user-facing prose in `README.md`, `.bat`, script output, and PowerShell comments is Portuguese-BR. Driver code comments, postmortems, and technical rationale in English. Commit messages in English. Keep this split when adding content.

## Toolchain — DO NOT deviate

- Compiler: **Visual Studio 2026 Community "VS 18"** (`C:\Program Files\Microsoft Visual Studio\18\Community`), MSVC 14.51+. VS 2022 BuildTools alone works if the C++ workload is installed; without it, `02-compilar-driver.bat` falls through to VS 18.
- WDK: 10.0.22621 at `C:\Program Files (x86)\Windows Kits\10\...\10.0.22621.0`. `signtool.exe` from the same SDK bin.
- Build: `.\02-compilar-driver.bat` from the repo root; wraps `nmake /f driver\makefile.mak`.
- **Signing is mandatory** — `makefile.mak` calls `signtool sign` after `link.exe` using self-signed cert `HWToolkit Test Cert 2026` (thumbprint `30310EE7644799431FFF099E1194817E813152B9`, valid until 2028-08-30, in host's `Cert:\CurrentUser\My`). If you remove or break the signing step, WDAC-enforced Win10 rejects the BOOT_START load and drops the box into an Automatic Repair loop with no bugcheck screen — see [`docs/postmortem-v4-phase5/incident-v407-driver-boot-regression.md`](docs/postmortem-v4-phase5/incident-v407-driver-boot-regression.md). If the cert expires, regenerate via `New-SelfSignedCertificate` and update `SIGN_SHA1` in `makefile.mak` and re-import public cert into target VM's `Cert:\LocalMachine\Root`.

## Standard commands

- Generate profile: `.\00-gerar-profile.bat` (writes `C:\ProgramData\.hwcfg\profile.json` v9+).
- Build driver: `.\02-compilar-driver.bat` (produces signed `driver\rstflt.sys`).
- Install driver: `.\03-instalar-driver.bat` (checks HVCI + testsigning, then `sc create` + UpperFilters + Parameters key). Requires reboot to activate.
- Arm SMBIOS-only replay: `.\scripts\spoof-smbios.ps1 -SmbiosOnly` — clears CPU state first.
- Arm CPU-only replay: `.\scripts\spoof-smbios.ps1 -CpuOnly` — clears SMBIOS state, sets `EnableCpuReplay=1`.
- Prep for BSOD dump collection: `.\scripts\prep-crashdump.ps1` (sets `AutoReboot=0` + `DedicatedDumpFile=C:\rstflt-dump.sys`); `-Restore` to revert.
- Audit consistency post-boot: `.\scripts\check-consistency.ps1` — decodes driver's `LastReplayStatus` breadcrumb, verifies driver version marker, cross-checks WMI/registry/SMBIOS/CPU/network.
- **v5.0.7 P0.5 preview (2026-09-02) — identity drift audit**: `.\scripts\check-identity-drift.ps1` — reads `profile.json` + samples live `ProcessorNameString` / `ComputerName` / `MachineGuid` / primary NIC MAC / `HWToolkit\SpoofCPUUserland` task state / emac-uuid file, prints per-field OK/DRIFT/WARN, exits 4 on drift. **`pre-test-checklist.bat` step [7/6] runs this automatically** — refuses to `--arm` if identity drifts. Motivated by ban #6 audit (Probe #1): CPU real "i5-11600K" leaked 16× via `HKLM\HARDWARE\...\CentralProcessor\0\ProcessorNameString` in the bare-metal test because Level A userland CPU spoof was never applied and `EnableCpuReplay` was never armed. `03-instalar-driver.bat` now prints a big yellow warning on completion pointing at `.\04b-aplicar-hwid-emac.bat --skip-disk --skip-volume --skip-usb --skip-hid` as the mandatory next step before ANY bare-metal test. Full P0.5 (`scripts/verify-arm.ps1` + IFEO wrapper on `rubinot_dx.exe` for launch-time hard-block) lands in a follow-up PR.
- Uninstall: `.\08-desinstalar-driver.bat --skip-fase16` — always the safe way to remove the driver (restores UpperFilters from backup key). Requires reboot to fully release.
- Aplicar Level A (EMAC-only, sem driver kernel): `.\04b-aplicar-hwid-emac.bat --skip-disk --skip-volume --skip-usb --skip-hid` - modo recomendado default contra EMAC-tier (RubinOT). Ver docs/emac-recon-v3.md.
- Rollback Level A userland: `.\08b-rollback-userland.bat` - reverte MachineGuid/ComputerName/etc dos backups .hwcfg + unregistra HWToolkit\SpoofCPUUserland task. Nao toca driver.
- Arm Track D (v5.0.0+, Cm registry callback kernel): `.\scripts\track-d-arm.ps1 -Enable` — grava `EnableRegCallback=1` + `RegCallbackSeed` em `Services\RstFlt\Parameters`. Precisa driver ja instalado. Reboot para o callback armar. Desde v5.0.4: gate por image name inline (`PsGetProcessImageFileName + _strnicmp "rubinot"`) — sem PID plumbing, sem `-SetPid`, sem `RubinOtPid`. Para probar em VM: spawnar qualquer processo com nome comecando `rubinot*` (`Copy-Item reg.exe rubinot_probe.exe; .\rubinot_probe.exe query ...`).
- Diagnose Track D: `.\scripts\track-d-arm.ps1 -Diagnose` — decode `LastCallbackStatus` (tag/status) + `CallbackHitCount`. Também disponível via `.\scripts\check-consistency.ps1` (agora inclui bloco Track D).
- **v5.0.5 Phase 1 (2026-09-01)**: descriptor-table refactor + BTH (`Enum\BTH\Dev_*`) + STORAGE\Volume (`Enum\STORAGE\Volume\{GUID}#offset`, boot-volume offset-zero protegido) enum-name rewriters. `CallbackHit_BTH`/`CallbackHit_Storage` passam de reservados a WIRED. Sem novo arm flag (o gate/seed são os mesmos; `EnableValueReadRewrite` é Phase 2). VM sanity harness: [`scripts/phase1-sanity-test.ps1`](scripts/phase1-sanity-test.ps1) — inspeciona os filhos reais de BTH/STORAGE\Volume na VM vs o gate do driver (valida a premissa de shape) + SCSI regression check. Spec: [`docs/track-d-v505-value-handler-kickoff.md`](docs/track-d-v505-value-handler-kickoff.md) §4.
- **v5.0.5 Phase 2 (2026-09-01) — value-read handler (fix aritmeticamente dominante)**: novo handler `RegNtPostQueryValueKey` que reescreve o DATA dos valores fingerprint (`HardwareID`/`CompatibleIDs`/`DeviceDesc`/`FriendlyName`/`Mfg` de SCSI/PCI/BTH/STORAGE + `EDID`) IN PLACE, consistente byte-a-byte com os sinteticos do name-side (mesmos dominios FNV). Motivo: EMAC le por nome via `RegQueryValueEx`, NAO enumera — o name-side (Phase 0/1) nunca dispara pra esse padrao. Arm: `.\scripts\track-d-arm.ps1 -EnableValueRewrite` (grava `EnableValueReadRewrite=1`; precisa `EnableRegCallback` ja armado; hot-toggle via tap, sem reboot). EDID e opt-in SEPARADO via `-Edid` (`EnableEdidValueRewrite=1`) — NAO usar junto com `spoof-edid-full.ps1` (double-spoof). Desarmar: `-DisableValueRewrite`. Diagnose novos counters `CallbackValHit_SCSI/_PCI/_BTH/_Storage/_Edid` + `CallbackNonRubiValueMatch` via `-Diagnose` / `check-consistency.ps1`. MachineGuid/ComputerName/CPU ficam FORA (userland Level A ja cobre; kernel double-spoofaria). USB/HID sem value row (serial so vaza via instance-ID, nao via valor). Writeup: [`docs/postmortem-v5-track-d/incident-v505-phase2-implementation.md`](docs/postmortem-v5-track-d/incident-v505-phase2-implementation.md). Spec: [`docs/track-d-v505-value-handler-kickoff.md`](docs/track-d-v505-value-handler-kickoff.md) §5. Recipe value-side: [`docs/track-d-name-recipe.md`](docs/track-d-name-recipe.md) §8.
- **Bare-metal ban #6 (2026-09-02, v5.0.6 Phase 2 armed) — file-based leak vector identified**: sessao gameplay ~12min pos-login com todos os 3 gates armed. Track D callback mecanicamente perfeito (357M invocations, 0 BSOD, `LastCallbackStatus=OK`, 204 substring rewrites landed em PCI/SCSI/BTH HardwareID/CompatibleIDs). MAS: **`SynthHit_*=0` across all 9 counters** (EMAC nao le DeviceDesc/FriendlyName/Mfg via NtQueryValueKey — hipotese v5.0.6 falsificada) + **measure-first triplet=0** (nem LocationInfo/LocationPaths/ContainerID — CRITICAL#4 falsificada). Procmon workflow (4-lens+synth, 5 agentes) identifica vetor definitivo: **`C:\Windows\System32\drivers\rstflt.sys` foi lido AS-A-FILE por `rubinot_dx.exe`** as 18:47:35 (~11min into session, 36 file-system ops em burst de 35ms, `ReadFile Offset:0 Length:89360` x2 = full binary consumed). Deliberate targeting confirmado (probes `C:\Program Files (x86)\RubinOT 2.0\rstflt.sys` PRIMEIRO -> "rstflt" hard-coded em known-bad-driver name list). Ban server-side 5.5min pos-file-read = pipeline async (client uploada hash+signer -> backend correlate -> ban). **Cm callback (altitude 321000) NAO alcanca IoManager/FltMgr** = arquitetural. v5.0.6 Phase 2 KEPT SHIPPED (defense-in-depth, dormant nao wrong). **v5.0.6 Phase 2.1 (USB+HID class-code hashmap) DEPRIORIZADA indefinidamente** (expandir Cm callback amplia superficie que EMAC nao exercita). Roadmap pivota para **v5.0.7 P0 = `FltRegisterFilter` filesystem minifilter** que retorna `STATUS_OBJECT_NAME_NOT_FOUND` em reads de `rstflt.sys` por gated `rubinot*`. Postmortem: [`docs/postmortem-v5-track-d/incident-v506-phase2-ban-driver-file-read.md`](docs/postmortem-v5-track-d/incident-v506-phase2-ban-driver-file-read.md). Kickoff v5.0.7: [`docs/track-d-v507-filesystem-minifilter-kickoff.md`](docs/track-d-v507-filesystem-minifilter-kickoff.md). Procmon capture: `C:\Users\xyrlan\AppData\Local\Temp\rubinot_flush_uuid*.pml` (2GB main + 6 overflow chunks) + 8.7GB CSV.
- **v5.0.6 Phase 2 (2026-09-02)**: OEM string synthesizer DISPATCH wired. Extends `TRACKD_VALUE_DESCRIPTOR` additively com `SynthValueNames + Synthesizer` fields; 3 per-class synthesizers (SCSI + PCI + BTH; USB + HID **DEFERRED** to Phase 2.1 apos adversarial review CRITICAL#1/#2 - parent-only USB inference colapsava mouse → "Synaptics Fingerprint Reader"). Splice em `TrackDHandlePostQueryValue`: on gated `rubinot*` caller reading DeviceDesc / FriendlyName / Mfg em classified SCSI/PCI/BTH parent, sintetiza da MESMA row da MESMA pool (cross-value brand coherence garantida via subSeed = FNV1a64(seed, className), NAO valueName), escreve no buffer do caller + updates BOTH `KEY_VALUE_*_INFORMATION.DataLength` AND `*pre->ResultLength`. PCI synth **sub-filtra por VenHex-match-real-VEN** (post-review CRITICAL#3): real Nvidia PDO (VEN_10DE) so pode virar RTX A4000/Tesla T4. PCI classmap workitem (populado OUTSIDE Cm lock via `ExInitializeWorkItem + ExQueueWorkItem` - post-review HIGH#8 pois `IoAllocateWorkItem` precisa `DeviceObject` que e NULL em BOOT_START DriverEntry). Todos os 9 `SynthHit_*` + 4 `Synth*Bail` counters wirados; SynthHit ortogonal a ValHit (synth path bumpa so `SynthHit_<class>_<name>` + `g_TrackDHitCount`, NAO `desc->HitCounter`). Nova hitkind `TRACKD_HITKIND_VALUE_GATED_SYNTH=4` no ring + decoders em `check-consistency.ps1` e `-Diagnose`. Sem novo arm flag (piggy-backs no `EnableValueSynth` do Phase 0, hot-toggle ja wirado). Sanity harness: `.\scripts\phase3-sanity-test.ps1` (PS mirror da formula + 19-row SCSI pool literal + byte-exact + cross-value-coherence + isolation + hive-non-persistence + hot-toggle round-trip). BUILD-MARKER `RstFlt-v5.0.6-BUILD-MARKER` inalterado; `rstflt.sys` 79632 → 89360 bytes. Writeup: [`docs/postmortem-v5-track-d/incident-v506-phase2-implementation.md`](docs/postmortem-v5-track-d/incident-v506-phase2-implementation.md). Spec: [`docs/track-d-v506-oem-string-synthesizer-kickoff.md`](docs/track-d-v506-oem-string-synthesizer-kickoff.md) §5.
- **v5.0.6 Phase 1 (2026-09-02)**: OEM string synthesizer INVENTORY curation. `driver/trackd_inventory.h` novo (832 linhas) com 78 rows enterprise-plausiveis (SCSI 19 / PCI 13 / USB 13 / HID 13 / BTH 13 / STORAGE 7 - todos primos pra reduzir bias no FNV % rowCount). Row shape = **uma-row-por-device** com (Vendor / Product / Rev / DeviceDesc / FriendlyName / Mfg) agrupados - todos value-reads no mesmo parent voltam com colunas da MESMA row, garantindo coerencia cross-value (o gap que EMAC pegou no ban v5.0.5 Phase 2). Selection: `subSeed = FNV1a64(seed, className); rowIndex = ((FNV1a64(subSeed, parentPathHash) >> 32) % rowCount)` (documented no header prologue; NAO XORs valueName). Curation bias: enterprise (Micron 5300/7400/9300, Samsung PM, Kioxia CD/CM, Intel D-series, Mellanox ConnectX-6, Intel X710, NVIDIA RTX A4000, AMD Radeon PRO W6800, Intel DC GPU Flex 170, HP LOGICAL VOLUME, DELL PERC H755); anti-consumer (NO Kingston SA400 / ADATA XPG / Crucial / WD Blue / GTX/RTX consumer / ASMedia / Realtek consumer). PCI Q2 (kickoff §10) RESOLVIDA -> **Option A (workitem hash-map)**: Phase 2 vai schedular `IoAllocateWorkItem` de `DriverEntry` que walk `Enum\PCI` OUTSIDE Cm callback lock, popula 256-slot FNV1a hash map com `TRACKD_PCI_CLASSHINT_*` (enum ja landed no header). Callback consulta O(1). Nenhuma nova flag de arm; **hot path do driver idem Phase 0** (rstflt.c gains only `#define TRACKD_INVENTORY_IMPL` + `#include "trackd_inventory.h"` pra validar sintaxe no build atual - linker inclui as literals no .rdata, `rstflt.sys` 61712->79632 bytes = +17920, inert ate Phase 2 dereferenciar); **nenhum VM cycle nesta PR** (nao tem superficie de callback pra medir). Phase 2 wira dispatch (~500-800 LOC C: extensao de `TRACKD_VALUE_DESCRIPTOR` com Synthesizer field + 6 synthesizer callbacks per-classe + workitem body + 9 `SynthHit_*` counter increments). Writeup: [`docs/postmortem-v5-track-d/incident-v506-phase1-implementation.md`](docs/postmortem-v5-track-d/incident-v506-phase1-implementation.md).
- **v5.0.7 Phase 0 (2026-09-02) — filesystem minifilter SCAFFOLDING (dormant, arm-gated)**: extende rstflt.sys com `FltRegisterFilter` no altitude **408000** (TEST-ONLY, FSFilter Activity Monitor group), single-driver dual-altitude. Registration scheduled via `DelayedWorkQueue` workitem (`TrackDFsFilterArmWorker`) porque nosso Group=`"PnP Filter"` carrega ANTES de FSFilter Infrastructure (fltmgr.sys) — sync `FltRegisterFilter` de DriverEntry retornaria `STATUS_NOT_FOUND`. **A registration é GATED em `EnableFsFilter=1` (adversarial review CRITICAL#1)** — publicar nosso nome + altitude via `fltmc filters` antes de Phase 1 wirar o hide widenaria a superficie de deteccao sem defesa. Boot arm roda iff flag=1 no boot; hot-toggle 0→1 via tap schedula o worker on demand. Instances subkey (`Services\RstFlt\Instances\RstFlt Instance` com `DefaultInstance` + `Altitude` + `Flags`) escrita programaticamente de DriverEntry via Zw* incondicionalmente (03-instalar-driver.bat inalterado; inert ate FltMgr ler durante FltRegisterFilter). 3 IRP preops (CREATE/READ/DIRECTORY_CONTROL) todos retornam `FLT_PREOP_SUCCESS_NO_CALLBACK` (Phase 1 wira hide + measure-first counters). `InstanceSetup` gates em `FLT_FSTYPE_NTFS + FILE_DEVICE_DISK_FILE_SYSTEM` (adversarial HIGH#2). `InstanceQueryTeardown` retorna `STATUS_SUCCESS` em Phase 0 (adversarial MEDIUM#3 — refusing detach sem hide state seria fingerprint). `FilterUnloadCallback` trata `FLTFL_FILTER_UNLOAD_MANDATORY` limpando estado (adversarial LOW#4) + retorna `STATUS_FLT_DO_NOT_DETACH` para unload voluntario (matches BOOT_START contract). Novo arm flag `EnableFsFilter` (REG_DWORD, hot-toggle via tap com companion arm-schedule; sem reader em P0). Novo breadcrumb `LastFsFilterStatus` (tag+status packing separado do `LastCallbackStatus`, tags 0x01 INSTANCES-WRITE-FAIL / 0x02 FLT-REGISTER-FAIL / 0x03 FLT-START-FAIL / 0x04 ARM-OK / 0x05 ARM-NULL-DRVOBJ / 0x06 MANDATORY-UNLOAD / 0x07 ARM-GATED-OFF). 12 novos REG_DWORDs em Parameters (`FsFilterRegistered`, `FsFilterInstanceCount`, 6 dormant counters, 3 measure-first probes) persistidos por `TrackDFlushWorker` extendido. Master include mudou `<ntddk.h> → <fltKernel.h>` (superset, evita redefinition PEPROCESS/PETHREAD). Makefile: `fltmgr.lib` adicionado + `/wd4324` (WDK fltKernel.h padded-struct warning storm) suprimido. rstflt.sys 89360 → 93968 bytes. Arm: `.\scripts\track-d-arm.ps1 -EnableFsFilter` (agora TAMBEM triggera FltRegisterFilter via hot-toggle). Disarm: `-DisableFsFilter` (nao desregistra em P0). Diagnose: `-Diagnose` (renderiza LastFsFilterStatus decodificado + os 12 novos counters). Kickoff: [`docs/track-d-v507-filesystem-minifilter-kickoff.md`](docs/track-d-v507-filesystem-minifilter-kickoff.md). Writeup: [`docs/postmortem-v5-track-d/incident-v507-phase0-implementation.md`](docs/postmortem-v5-track-d/incident-v507-phase0-implementation.md).
- **v5.0.6 Phase 0 (2026-09-02)**: OEM string synthesizer SCAFFOLDING (instrumentation-only landing; nenhum synthesizer code + nenhum descriptor extension). Motivacao: ban #5 bare-metal (2026-09-02) reproduziu o outcome "value handler works mas leak lives elsewhere" — `HardwareID` reescrito byte-exact via Phase 2, MAS `DeviceDesc`/`FriendlyName`/`Mfg` continuam vazando OEM cleartext ("NVIDIA GeForce RTX 3070" etc). Phase 2 do v5.0.6 (proxima) vai wirar um synthesizer independente por (device class, value name). Phase 0 landa: ring 16→128 slots (janela forense de ~11s vs ~1.3s do v5.0.5); 9 `CallbackSynthHit_*` + 4 `CallbackSynth*Bail` counters (declarados, wired em Phase 2); 3 measure-first counters `CallbackValHit_LocationInfo`/`_LocationPaths`/`_ContainerID` (WIRED em Phase 0 — mede se EMAC le esses value names antes de gastar LOC em Phase 2); flag `EnableValueSynth` com hot-toggle via tap (sem reader ainda). Arm: `.\scripts\track-d-arm.ps1 -EnableSynth` — grava `EnableValueSynth=1`. Efeito imediato via tap; SEM efeito observavel em Phase 0 (nenhum reader ainda). Desarmar: `-DisableSynth`. Spec: [`docs/track-d-v506-oem-string-synthesizer-kickoff.md`](docs/track-d-v506-oem-string-synthesizer-kickoff.md). Writeup: [`docs/postmortem-v5-track-d/incident-v506-phase0-implementation.md`](docs/postmortem-v5-track-d/incident-v506-phase0-implementation.md). Motivacao: [`docs/postmortem-v5-track-d/incident-v505-phase2-ban-cleartext-oem-strings.md`](docs/postmortem-v5-track-d/incident-v505-phase2-ban-cleartext-oem-strings.md).
- Disable Track D (sem uninstall driver): `.\scripts\track-d-arm.ps1 -Disable` — grava `EnableRegCallback=0`. Efeito imediato (hot path pass-through), callback continua registrado no kernel.
- Boot recovery from WinRE: `.\09-recuperar-boot.bat` (offline registry surgery to remove `RstFlt` from UpperFilters and delete the service).

**Do NOT invoke** `sc stop RstFlt ; sc delete RstFlt ; Restart-Computer -Force` in one shot without first removing `RstFlt` from `HKLM:\SYSTEM\CurrentControlSet\Control\Class\{4d36e967-...}\UpperFilters` — this reproduces the STOP 0x7B failure mode documented in [`incident-v402-signature-plus-filter.md`](docs/postmortem-v4-phase5/incident-v402-signature-plus-filter.md) (UpperFilters walk points at deleted service → `CM_PROB_FAILED_ADD` → 0x7B). `08-desinstalar-driver.bat` does the correct order automatically.

## Testing on the Hyper-V dev VM

- Target VM: `Ambiente de desenvolvimento do Windows 10` (windev-image, Win10 Enterprise, Gen 2 UEFI, storvsc, 8 vCPU, WDAC enforced mode 2, testsigning ON, HVCI OFF, Secure Boot OFF).
- **Before starting the VM for any arming test**, disable heartbeat + KVP integration services on the host:
  ```powershell
  Disable-VMIntegrationService -VMName 'Ambiente de desenvolvimento do Windows 10' `
      -Name 'Pulsação','Troca do Par Chave-Valor'
  ```
  Otherwise the Hyper-V watchdog resets the guest at ~52-56s post-Winlogon when KVP encounters the modified registry state (Bug 4, closed as H2 host-side reset — see `incident-v405-vm-pipeline-validation.md` header). Names above are Windows PT-BR; EN names are `Heartbeat` and `Key-Value Pair Exchange`.
- Restore config re-enables both services on VM checkpoint restore — always redisable after `Restore-VMCheckpoint`.
- File transfer host → guest: `Copy-VMFile -Name '<vm>' -SourcePath <host> -DestinationPath <guest> -CreateFullPath -FileSource Host -Force`. Requires guest's `vmicguestinterface` service running (Manual startup, resets to Stopped on each reboot — the user starts it manually per session). Copy-VMFile **guest → host does not exist**; use base64-into-chat or SMB share for that direction.
- **CRITICAL — reboot the guest from INSIDE, never with `Restart-VM`/`Stop-VM` from the host during a driver install/uninstall.** With KVP+Heartbeat disabled, a host-side `Restart-VM -Force` lands as an **unclean shutdown** (Kernel-Power event 41) that does NOT flush the SYSTEM registry hive — so a just-written `sc create`/UpperFilters/Parameters (or a just-removed one) is **rolled back** to the last flush while the `.sys` FILE (NTFS-journaled) persists. Symptoms: after install → the `RstFlt` service + UpperFilters entry + Parameters key vanish but `rstflt.sys` stays (driver silently doesn't load); after uninstall → UpperFilters still points at `RstFlt` whose `.sys` was deleted → `CM_PROB_FAILED_ADD`/STOP 0x7B → **Automatic Repair loop** (exactly the v407 failure mode, but caused by the reboot method, not signing). Fix: reboot via `Invoke-Command -VMName … { shutdown /r /t 5 /f }` (a clean OS shutdown flushes the hive). Discovered 2026-09-02 during the Phase 2 VM cycle after two false boot-failures.
- **PowerShell Direct automation (2026-09-02):** the whole VM cycle can be driven from the host via `Invoke-Command -VMName '<vm>' -Credential $cred` (account `user`, blank password) once the guest has `HKLM\SYSTEM\CurrentControlSet\Control\Lsa\LimitBlankPasswordUse=0` set (default 1 BLOCKS blank-password network logon → "A credencial é inválida" even though the password is truly blank). This is a **one-time** guest-console command per pristine checkpoint; `clean-v505-phase2-armed` already has it + `AutoAdminLogon=1` baked in, so cycles from that checkpoint are hands-free. Run interactive install/uninstall `.bat`s non-interactively with stdin from NUL: `cmd /c '03-instalar-driver.bat < nul 2>&1'` (their `pause` returns immediately). The guest repo is at `C:\hwtoolkit`; probes/harnesses use `C:\hwtoolkit`.
- Working checkpoints on the VM (as of PR #18):
  - `clean-v507-phase0-armed` (target after v5.0.7 P0 VM cycle) — driver v5.0.7 Phase 0 installed (93968 bytes signed) + `EnableFsFilter=1` + all prior 3 gates armed (EnableRegCallback + EnableValueReadRewrite + EnableValueSynth). VM cycle roda em dois passos: (1) **Run A default install SEM `-EnableFsFilter`**: `LastFsFilterStatus` = `07 ARM-GATED-OFF`, `FsFilterRegistered = 0`, `FsFilterInstanceCount = 0`, `fltmc filters` NAO mostra RstFlt (adversarial CRITICAL#1 fix comprovado); (2) **Run B pos `-EnableFsFilter`**: `LastFsFilterStatus` = `04 ARM-OK`, `FsFilterRegistered = 1`, `FsFilterInstanceCount >= 1` (NTFS-on-disk only per HIGH#2 fix), todos os 6 dormant + 3 measure-first probes = 0, hot-toggle `-DisableFsFilter` clean via tap sem reboot (registration persiste, so o flag muda). Base para Phase 1 iteration (PreCreate hide + measure-first counter wiring). Bare-metal single-ship recomendado sem `-EnableFsFilter` (Run A shape) — Phase 0 dormant + registration gated = zero detection-surface widening.
  - `clean-v506-phase2-armed` (2026-09-02) — **driver v5.0.6 Phase 2 installed (89360 bytes signed) + all 3 gates armed (EnableRegCallback=1 + EnableValueReadRewrite=1 + EnableValueSynth=1), boot-validated (115M callback invocations, 0 BSOD, `LastCallbackStatus=OK`, `CallbackHitCount=10`).** Sanity test PASSED all 4 HARD-checks: (1) cross-value coherence — gated `rubinot_probe` reads DeviceDesc + FriendlyName + Mfg all from pool row 7 (`INTEL SSDSC2KG960G8 SCSI Disk Device` / `Intel Corporation`); (2) isolation — non-gated `reg.exe` returns real `@disk.inf,%disk_devdesc%;Disk drive`; (3) hive non-persistence — `Get-ItemProperty` non-gated sees real; (4) PS mirror byte-exact — recipe computed row 7 = observed row 7 using canonical `\REGISTRY\MACHINE\SYSTEM\ControlSet001\...` parent form. Ring buffer decoder shows `128/128 slots (0 enum-gated, 2 value-gated, 8 value-synth, 118 non-rubi)` — new `TRACKD_HITKIND_VALUE_GATED_SYNTH=4` correctly bucketed. All 4 `Synth*Bail` counters = 0. Hot-toggle `-DisableSynth`/`-EnableSynth` clean via tap. Parent = `clean-v506-phase0-armed`. State: clean shutdown pos-sanity test. **Ideal base para bare-metal single-ship + Phase 2.1 iteration** (USB + HID class-code hash-map).
  - `clean-v506-phase0-armed` (2026-09-02) — **driver v5.0.6 Phase 0 installed + all 3 gates armed (EnableRegCallback=1 + EnableValueReadRewrite=1 + EnableValueSynth=1), boot-validated (17.5M callback invocations, 0 BSOD, `LastCallbackStatus=OK`).** Sanity test PASSED byte-exact: `Msft`->`FDF2` + `Virtual_Disk`->`F381B244E994` intact (Phase 2 substring rewriter unchanged); `CallbackValHit_LocationInfo=1` + `CallbackValHit_ContainerID=1` (v5.0.6 measure-first counters wired); hot-toggle `-DisableSynth`/`-EnableSynth` clean via tap; ring buffer 128 slots decoder validated (`83/128 slots` post-probe, 1 value-gated + 82 non-rubi enum). All 9 SynthHit_* + 4 Synth*Bail counters render dormant (Phase 2 will wire increments). BUILD-MARKER embutido `RstFlt-v5.0.6-BUILD-MARKER`. Parent = `clean-v505-phase2-armed`. State: clean shutdown pos-sanity test. **Ideal base para Phase 1 iteration** (curadoria de inventarios por classe + PCI sub-classification por class-code).
  - `clean-v505-phase2-armed` (2026-09-02) — **driver v5.0.5 Phase 2 installed + EnableRegCallback=1 + EnableValueReadRewrite=1 (value-side armed), boot-validated (23M callback invocations, 0 BSOD).** `phase2-sanity-test.ps1` PASSED: gated `rubinot_probe` HardwareID rewritten byte-exact (`Msft`→`FDF2`, `Virtual_Disk`→`F381B244E994`, matching the PS recipe), non-gated sees real, hive untouched (non-persistent). Also has `LimitBlankPasswordUse=0` + `AutoAdminLogon=1` for hands-free PS Direct. **Ideal base para Phase 2 iteration + bare-metal prep.** State: clean shutdown pos-sanity test.
  - `clean-v505-phase0-armed` (2026-09-01) — **driver v5.0.5 installed + EnableRegCallback=1 + Phase 0 instrumentation validated (SCSI end-to-end, ring buffer + per-path counters + NonRubi counter all persist). Ideal base para iteracao Phase 1 (BTH + STORAGE\Volume + descriptor-table refactor) e Phase 2 (RegNtPostQueryValueKey handler).** State: shutdown limpo pos-sanity test. Ver [`scripts/phase0-sanity-test.ps1`](scripts/phase0-sanity-test.ps1) pro test harness.
  - `clean-v504-armed-track-d-tested` (2026-09-01) — driver v5.0.4 installed + EnableRegCallback=1 + probed once (HitCount=1). Superseded pelo v5.0.5 checkpoint acima; mantido para regressao contra v5.0.4 se necessario.
  - `clean-v501-armed-track-d` (2026-09-01) — driver v5.0.1 armed, superseded pelo v5.0.4 checkpoint acima.
  - `clean-v409-installed` — driver v4.0.9 signed installed, no arming. Base historica.
  - `clean-no-driver` — no `RstFlt` registered anywhere. Base for install-from-scratch tests; parent de todos os `clean-v50X-*` checkpoints.
  - `pre-v406-test` — the last v4.0.4 checkpoint kept around for the historical binary if needed.

## Gotchas

- **PowerShell `sc` is not `sc.exe`.** In PS 5.1, bare `sc` is an alias for `Set-Content`. When invoking the Service Control Manager from PS, always use `sc.exe` explicitly. From within `.bat` files, `sc` resolves to `sc.exe` correctly.
- **PowerShell script files must be ASCII** (or UTF-8 with BOM). PS 5.1 default reads BOM-less UTF-8 as Windows-1252 and multi-byte chars (em-dash, accents) break the parser. Scripts in this repo are ASCII-only on purpose (see the comment header in `check-consistency.ps1`). If you edit, keep ASCII.
- **PE `TimeDateStamp` + Authenticode signature timestamp make SHA256 change every relink** — do not use SHA to validate driver identity. Use `check-consistency.ps1 Read-DriverVersionMarker` (regex-matches the embedded `RstFlt-v<version>-BUILD-MARKER` string kept by `#pragma comment(linker, "/INCLUDE:RstFltVersion")` in `rstflt.c`) or `signtool verify /pa /v rstflt.sys`.
- **`mssmbios.sys` is `Start=1` (SYSTEM_START) on stock Windows**, loading AFTER our BOOT_START `RstFlt` — `ZwOpenKey` on `\Registry\Machine\SYSTEM\CurrentControlSet\Services\mssmbios\Data` at DriverEntry time returns `STATUS_OBJECT_NAME_NOT_FOUND`. The driver's `WriteLastReplayStatus` breadcrumb records tag `0x04 MSSMBIOS-OPEN-FAIL` in `LastReplayStatus` when this fires — that is the expected steady state on this Hyper-V VM, not a bug.
- **WMI (Win32_ComputerSystemProduct, Win32_BaseBoard, Win32_SystemEnclosure) serves from mssmbios's in-kernel firmware cache**, not from `HKLM\...\mssmbios\Data\SMBiosData`. Registry writes into that key have no effect on WMI queries — confirmed empirically 2026-08-31. Real spoof requires IRP-level interception on `\Driver\mssmbios` or a UMDF WMI provider shadow, both scheduled in [`docs/roadmap-v41-wmi-intercept.md`](docs/roadmap-v41-wmi-intercept.md). Do NOT re-attempt the registry-write strategy without reading Bug 3 in `incident-v406-bug-triage.md`.
- **On a `03-instalar-driver.bat` failure with `CreateService FAILED 1072`** (service marked for deletion): a prior `sc delete` did not fully commit. Reboot once, run `03-instalar-driver.bat` again — it succeeds cleanly on a fresh SCM state.
- **Track D `RstRegistryCallback` MUST NOT call `Zw*` registry APIs from inside its body** — MSDN explicitly warns of deadlock under the CM-internal lock. Any breadcrumb persistence goes through `WriteLastCallbackStatus` → `TrackDFlushWorker` which runs on `DelayedWorkQueue` OUTSIDE the callback stack. Same rule for the `PsSetCreateProcessNotifyRoutineEx` callback (safer, but keep the contract uniform). Config (seed, enable gate, override PID) is read once at DriverEntry into file-scope globals and mutated only by the callback's own `RegNtPreSetValueKey` tap on our own Parameters key — no external Zw calls.
- **Track D uses altitude `321000` for `CmRegisterCallbackEx`** — this is a TEST-ONLY altitude never allocated by Microsoft. Fine for dev boxes; if this ever ships beyond the maintainer's own systems, requisition an official allocation via MSDN ALTITUDE registry before wider distribution.

## Documentation map

- `README.md` — the main user-facing changelog and setup guide, Portuguese, ~55KB. Everything user-facing goes there.
- `docs/postmortem-v4-phase5/` — incident writeups by version. Read the latest (`incident-v407-driver-boot-regression.md`) first for the current stable-driver context; the `incident-v405-vm-pipeline-validation.md` header shows the closure status of every open bug.
- `docs/roadmap-v41-wmi-intercept.md` — the next architectural pivot for real WMI-visible spoofing. Explicit PatchGuard warnings against the naive `DriverObject` dispatch swap. UMDF WMI provider shadow to test first.
- `docs/emac-recon-v2.md`, `docs/fase2-kickoff.md`, `docs/fase2-track-a-windows-test-kickoff.md` — anti-cheat threat model + phase planning.

## Style

- Every driver change requires a corresponding changelog block at the top of `driver/rstflt.c` (`v4.0.x - ...`) — do not merge without.
- Every non-trivial toolkit-behavior discovery gets a new `docs/postmortem-v4-phase5/incident-v40X-*.md`. Reference the file from `README.md` and from the corresponding `v4.0.x` changelog block in the driver.
- Commit messages end with `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` when the assistant did substantial work.
- PR bodies end with `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
- Squash-merge is the convention (`gh pr merge <n> --squash --delete-branch`).
